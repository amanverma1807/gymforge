{
  "name": "gullible-group",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && npx wrangler pages deploy dist/client --project-name gymforge-online --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^6.4.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.99.0"
  },
  "overrides": {
    "vite": "^7"
  }
}